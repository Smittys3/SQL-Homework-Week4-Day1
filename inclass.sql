-- INTO TO SQL, SELECT ALL FROM ACTOR TABLE
SELECT * FROM actor;

-- QUERY FOR THE FIRST ANEM AND LAST NAME
SELECT first_name, last_name 
FROM actor;

-- BE more specific..look for first name which equal to "Nick"

SELECT last_name 
FROM actor 
WHERE first_name = 'Nick';

-- 


